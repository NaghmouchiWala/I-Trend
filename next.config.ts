import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['www.tunisianet.com.tn','mk-media.mytek.tn', 'media.mytek.tn'],
    
  },
  /* other config options here */
};

export default nextConfig;
